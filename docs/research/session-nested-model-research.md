# 会话嵌套模型 - 调研报告

> **问题**: DivineSense 的普通模式、极客模式和进化模式存在会话上下文割裂
> **方案**: 母会话-子会话嵌套模型（追加式输入 + 完整 Q+A 保存）
> **状态**: ✅ 已创建 Issue [#57](https://github.com/hrygo/divinesense/issues/57)
> **日期**: 2026-02-03 (v2.0 基于 commit 567bbe3)

---

## 1. 问题描述

### 当前架构的三重割裂

| 维度 | 普通模式 | 极客模式 (Geek Mode) | 进化模式 (Evolution Mode) |
|:-----|:---------|:---------------------|:--------------------------|
| **会话存储** | `conversation_context` 表 (PostgreSQL) | Claude Code CLI 内存 (`~/.claude/sessions/`) | Claude Code CLI 内存 |
| **SessionID 来源** | DivineSense 后端生成 | UUID v5(ConversationID) 映射 | 短 UUID (8 字符) |
| **历史管理** | DivineSense `session.Service` | CC CLI 自动管理 | CC CLI 自动管理 |
| **前端显示** | ✅ 完整同步 | ⚠️ 仅显示流式输出 | ⚠️ 仅显示流式输出 |
| **上下文连续性** | ✅ 数据库持久化 | ✅ CC 内部持久化 | ✅ CC 内部持久化 |
| **跨模式共享** | ❌ 与 CC 隔离 | ❌ 与普通模式隔离 | ❌ 与其他模式隔离 |

### 割裂的具体表现

1. **用户体验割裂**
   - 普通模式切换到极客模式 → 上下文丢失
   - Geek/Evolution 会话内容不出现在 DivineSense 的 `ai_conversation` 表中
   - 前端侧边栏无法显示 Geek/Evolution 会话历史

2. **数据模型割裂**
   ```
   DivineSense conversation_context 表:
   ┌─────────────┬─────────┬──────────────┬────────────────────┐
   │ session_id  │ user_id │ agent_type   │ context_data (JSON) │
   ├─────────────┼─────────┼──────────────┼────────────────────┤
   │ "ds_xxx"    │ 123     │ "memo"       │ [...messages]      │
   └─────────────┴─────────┴──────────────┴────────────────────┘

   Claude Code CLI (~/.claude/sessions/):
   ┌──────────────────────────────┬─────────────────────────────┐
   │ divinesense:conversation:{id} │ [...CC internal messages]  │
   └──────────────────────────────┴─────────────────────────────┘
   ```

3. **代理能力割裂**
   - 灰灰/时巧/折衷 无法访问 Geek/Evolution 产生的上下文
   - Geek/Evolution 无法访问 DivineSense 的笔记/日程检索结果

---

## 2. 解决方案：会话嵌套模型

### 核心概念

```
┌─────────────────────────────────────────────────────────────┐
│                    母会话 (Normal Mode)                     │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ User: "帮我分析这段代码的性能问题"                       │ │
│  │ Assistant: "好的，切换到极客模式深入分析..."             │ │
│  │                                                        │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │ 🔄 子会话启动 (Geek Mode) - 黑盒执行               │  │ │
│  │  │   CC Session ID: divinesense:conversation:{id}    │  │ │
│  │  │   ... [分析、优化代码过程] ...                      │  │ │
│  │  │                                                   │  │ │
│  │  │   产出: code_review_report.md                     │  │ │
│  │  │   产出: optimized_solution.go                     │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │                                                        │ │
│  │ 📝 子会话总结 (嵌入母会话消息流)                         │ │
│  │ Assistant: "极客模式分析完成：                         │ │
│  │   • 发现 3 个性能瓶颈                                  │ │
│  │   • 已生成优化方案 (见附件)                            │ │
│  │   • [下载产出文件]                                    │ │
│  │                                                        │ │
│  │ User: "把优化后的代码应用到项目中"                     │ │
│  │ Assistant: "好的..."                                    │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 设计原则

1. **追加式输入** - 子会话支持多 Q 累加 → 单 A 响应（CC 有会话管理）
2. **完整保存** - Q&A 完整保存到 `ai_message`，不生成摘要
3. **不限制** - 追加的 Q 数量不限制，历史保留
4. **可追溯性** - 保留 cc_session_id，开发者可本地查看详情

### 核心差异：追加式 vs 累加式

| 维度 | **LLM 模式（普通/灰灰/时巧）** | **CC 模式（极客/进化）** |
|:-----|:---------------------------|:------------------------|
| **会话存储** | `conversation_context` 表 | `~/.claude/sessions/` 本地 |
| **上下文管理** | 每次请求发送完整 messages[] | CLI 内部持久化，追加输入 |
| **执行方式** | 累加式 (cumulative) | **追加式** (append) |
| **进程模型** | 无状态 | 持久进程 |
| **指令发送** | HTTP 请求 → LLM API | `WriteInput()` → Stdin |

---

## 3. 技术方案

### 数据模型

```sql
-- 扩展 ai_conversation 表
ALTER TABLE ai_conversation ADD COLUMN parent_conversation_id INTEGER;
ALTER TABLE ai_conversation ADD COLUMN session_mode VARCHAR(20); -- 'normal'|'geek'|'evolution'
ALTER TABLE ai_conversation ADD COLUMN cc_session_id VARCHAR(64); -- 仅用于引用

-- 索引
CREATE INDEX idx_conversation_parent ON ai_conversation(parent_conversation_id);
```

**不保留**：
- ❌ CC CLI 原始会话内容
- ❌ thinking/tool_use 详细过程
- ❌ artifacts 文件内容

**保留**：
- ✅ 子会话摘要（1-3 句话）
- ✅ cc_session_id（用于本地调试时追溯）
- ✅ 产出的文件路径列表

### 前端类型定义

```typescript
// 扩展 Conversation 类型
export interface Conversation {
  id: string;
  parentConversationId?: string; // 母会话 ID
  sessionMode?: 'normal' | 'geek' | 'evolution'; // 会话模式
  ccSessionId?: string; // CC CLI session ID

  // 现有字段...
  title: string;
  messages: ChatItem[];
  // ...
}

// 新增：子会话卡片数据
export interface SubsessionMetadata {
  id: string;
  mode: 'geek' | 'evolution';
  status: 'running' | 'completed' | 'failed';
  startTime: number;
  endTime?: number;
  summary?: string; // LLM 生成的摘要
  artifacts?: string[]; // 产出文件路径
}
```

### 事件流

```
┌─────────────────────────────────────────────────────────────────────┐
│ 事件流: 母会话 → 子会话 → 总结                                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│ 1. subsession_start                                                  │
│    { mode: "geek", cc_session_id: "xxx" }                           │
│    ↓ 前端显示 SubsessionCard (status: running)                      │
│                                                                      │
│ 2. thinking / tool_use / answer (原有流)                             │
│    ↓ 前端实时显示执行过程                                            │
│                                                                      │
│ 3. subsession_summary                                                │
│    { summary: "• 发现3个性能瓶颈...", artifacts: [...], cc_session_id }│
│    ↓ 前端更新 SubsessionCard (status: completed, 显示总结)           │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 前端组件

**SubsessionCard** - 子会话卡片展示

```tsx
<SubsessionCard
  subsession={{
    mode: "geek",
    status: "completed",
    summary: "• 重构了 data_pipeline.go，减少30%内存占用\n• 添加了单元测试覆盖",
    artifacts: ["optimized_pipeline.go", "test_coverage.html"]
  }}
  onViewDetails={(ccSessionId) => {/* 可选：打开详情 */}}
/>
```

---

## 4. 实现要点

### 后端

**新增服务**：`server/service/subsession/`

```go
type SubsessionService struct {
    store        *store.Store
    llmService   llm.Service // 用于生成总结
}

// CreateSubsession 创建子会话
func (s *SubsessionService) CreateSubsession(
    ctx context.Context,
    parentConversationID int32,
    mode string, // "geek" | "evolution"
) (*Subsession, error)

// FinalizeSubsession 结束子会话并生成总结
func (s *SubsessionService) FinalizeSubsession(
    ctx context.Context,
    subsessionID string,
) (*SubsessionSummary, error)
```

**总结生成 Prompt**：

```go
const summaryPrompt = `
以下是一次 Claude Code CLI 会话的执行记录：
%s

请用 1-3 句话总结这次会话：
1. 主要做了什么？
2. 产出了什么结果？
3. 是否有需要用户关注的事项？

输出格式：
• [要点1]
• [要点2]
...
`
```

### AI 代理更新

**GeekParrot / EvolutionParrot** - 新增子会话生命周期钩子：

```go
func (p *GeekParrot) ExecuteWithCallback(...) error {
    // 1. 通知前端：子会话启动
    callback(EventTypeSubsessionStart, map[string]any{
        "mode": "geek",
        "cc_session_id": p.sessionID,
    })

    // 2. 执行 CC Runner（原有逻辑）
    if err := p.runner.Execute(ctx, cfg, userInput, callback); err != nil {
        return err
    }

    // 3. 子会话结束，触发总结生成
    summary, err := p.subsessionSvc.FinalizeSubsession(ctx, p.sessionID)
    if err != nil {
        slog.Warn("Failed to generate subsession summary", "error", err)
    } else {
        // 4. 发送总结到母会话
        callback(EventTypeSubsessionSummary, map[string]any{
            "summary": summary.Text,
            "artifacts": summary.Artifacts,
            "cc_session_id": p.sessionID,
        })
    }

    return nil
}
```

---

## 5. 风险与缓解

| 风险 | 影响 | 措施 |
|:-----|:-----|:-----|
| LLM 总结质量不稳定 | 中 | 使用可靠模型 + 模板优化 + 失败时显示默认消息 |
| 子会话状态同步 | 中 | WebSocket 事件 + 心跳检测 |
| 产出文件路径解析 | 低 | 标准化工作目录 + 相对路径 |

---

## 6. 参考资料

- **CC Runner 异步架构**: `docs/specs/cc_runner_async_arch.md`
- **会话服务**: `plugin/ai/session/interface.go`
- **GeekParrot**: `plugin/ai/agent/geek_parrot.go`
- **EvolutionParrot**: `plugin/ai/agent/evolution_parrot.go`
- **前端上下文**: `web/src/contexts/AIChatContext.tsx`
- **Session Manager**: `plugin/ai/agent/session_manager.go`

---

## 7. Issue 链接

**[#55](https://github.com/hrygo/divinesense/issues/55)** - [feat] 会话嵌套模型 - 母会话与子会话(Geek/Evolution)的统一架构

---

*调研完成时间: 2026-02-03*
*调研者: Claude (Idea Researcher v3.1)*
