# Idea Researcher 使用示例

## 示例：番茄钟功能

### 用户输入
```
/idea-researcher 我想添加一个番茄钟功能
```

### 阶段 1: 理解与扩展

## 理解你的 Idea

**核心需求**：为 DivineSense 添加番茄钟（Pomodoro）计时功能

**关键问题**：
1. 番茄钟时长是否可自定义？（默认 25 分钟）
2. 是否需要统计功能（完成的番茄数、专注时长）？
3. 是否需要与笔记/日程集成？（如：番茄钟结束后自动创建日程）
4. 需要提醒通知吗？（浏览器通知 / 声音）

**扩展方向**：
- **基础版**：简单计时器，开始/暂停/重置
- **统计版**：记录每日/每周完成的番茄数
- **集成版**：与笔记关联（每个番茄关联到当前笔记）
- **AI 版**：AI 根据任务自动建议番茄数

**下一步**：请回答上述问题，或告诉我继续进入调研阶段

---

### 用户回复
```
默认25分钟，需要统计和浏览器通知，先做基础版
```

### 阶段 2: 深度调研

## 调研报告

### 技术可行性
- **可行性评级**: 高
- **技术方案**: 纯前端实现，使用 `setInterval` 计时，`localStorage` 持久化
- **架构影响**: 无影响，新增独立模块

### 用户价值
- **解决的问题**: 帮助用户保持专注，提高生产力
- **目标用户**: 所有需要专注工作/学习的用户
- **使用频率**: 高（日常工作场景）

### 竞品/最佳实践
| 产品 | 实现方式 | 可借鉴点 |
|:-----|:---------|:---------|
| Todoist | 集成番茄钟 | 与任务关联 |
| Notion | 独立番茄钟组件 | 简洁 UI |
| Forest | 游戏化体验 | 专注统计可视化 |

### 相关调研
- 无已有相关调研

### 实现复杂度
- **工作量估算**: 0.5 人周
- **风险点**:
  - 页面关闭后计时失效（使用 localStorage 时间戳恢复）
  - 浏览器通知权限处理
- **依赖项**: 无

### 相关 Issue
- 无相关 Issue
- 搜索现有: `gh issue list --repo hrygo/divinesense --search "tomato"` 无结果

---

### 阶段 3: 方案设计

## 功能方案

### 功能边界
**包含**：
- 25 分钟倒计时（可自定义 15-60 分钟）
- 开始/暂停/重置控制
- 短休息（5 分钟）/ 长休息（15 分钟）提示
- 浏览器桌面通知
- 每日完成的番茄数统计

**不包含**（未来迭代）：
- 与笔记关联
- AI 建议番茄数
- 跨设备同步

### 技术方案
**后端**：无

**前端**：
- 新增 `web/src/components/PomodoroTimer.tsx`
- 新增 `web/src/hooks/usePomodoro.ts`（计时逻辑）
- 新增 `web/src/pages/Pomodoro.tsx`（独立页面，可选）
- i18n: `pomodoro.*` keys

**数据存储**：
- `localStorage` 存储当前状态和统计数据

### 用户体验
**主要流程**：
1. 用户点击"开始番茄钟"
2. 25 分钟倒计时开始
3. 时间到：播放提示音 + 桌面通知
4. 自动进入 5 分钟短休息
5. 休息结束后提示开始下一个番茄

**边缘情况**：
- 页面关闭 → 重新打开时根据时间戳恢复剩余时间
- 浏览器不支持通知 → 降级为声音提示
- 用户离开页面 → 标题显示倒计时

### 风险与缓解
| 风险 | 影响 | 缓解措施 |
|:-----|:-----|:---------|
| 计时不准确（后台节流） | 中 | 使用 Date 差值计算而非单纯计数 |
| 通知权限被拒绝 | 低 | 降级为页面内提示 + 声音 |

---

### 阶段 4: 迭代修订

**请确认**：
1. 功能边界是否符合预期？
2. 有遗漏的重要细节吗？
3. 有希望调整的地方吗？

回复：
- "确认" → 进入 Issue 创建
- "修改 X" → 说明具体修改意见

---

### 用户回复
```
确认
```

### 阶段 5: 创建 Issue

**首先验证链接可访问性**：
```bash
# 使用 SKILL.md 中定义的 verify_links 函数验证链接
verify_links() {
    local url="$1"
    local status=$(curl -s -o /dev/null -w "%{http_code}" --max-time 5 "$url" 2>/dev/null)
    case "$status" in
        200|301|302|304) echo "✓ $url" ;;
        000|404) echo "✗ $url [无效，必须移除或替换]" ;;
        *) echo "⚠ $url [HTTP $status，需确认]" ;;
    esac
}

# 验证示例中的链接
verify_links "https://developer.mozilla.org/en-US/docs/Web/API/Notifications_API"
```

输出：
```
✓ https://developer.mozilla.org/en-US/docs/Web/API/Notifications_API
```

**链接验证通过后，使用 GitHub CLI 创建**：
```bash
gh issue create --repo hrygo/divinesense --title "[feature] 番茄钟功能" --body "$(cat <<'EOF'
## [feature] 番茄钟功能

### 问题描述
添加番茄钟（Pomodoro）计时功能，帮助用户保持专注，提高生产力。

### 当前行为
不存在

### 期望行为
用户可以启动 25 分钟倒计时，收到浏览器通知和提示音，记录每日完成的番茄数。

### 解决方案

#### 功能范围
**包含**：
- 25 分钟倒计时（可自定义 15-60 分钟）
- 开始/暂停/重置控制
- 短休息（5 分钟）/ 长休息（15 分钟）提示
- 浏览器桌面通知
- 每日完成的番茄数统计

**不包含**：
- 与笔记关联（未来迭代）
- AI 建议番茄数（未来迭代）

#### 技术方案
**后端变更**：无

**前端变更**：
- 新增 `web/src/components/PomodoroTimer.tsx`
- 新增 `web/src/hooks/usePomodoro.ts`（计时逻辑）
- 新增 `web/src/pages/Pomodoro.tsx`（独立页面，可选）
- i18n: `pomodoro.*` keys

**数据存储**：
- `localStorage` 存储当前状态和统计数据

#### 参考资源
- [Web Notifications API](https://developer.mozilla.org/en-US/docs/Web/API/Notifications_API)
- Todoist 番茄钟实现

### 实现复杂度
- **工作量估算**: 0.5 人周
- **风险等级**: 低

### 验收标准
- [ ] 25 分钟倒计时准确
- [ ] 开始/暂停/重置功能正常
- [ ] 浏览器通知工作
- [ ] 页面关闭后能恢复状态
- [ ] 每日统计正确记录
- [ ] `make check-all` 通过
- [ ] i18n 双语完整

### 依赖项
- 无

### 标签
`feature-web` `complexity-small` `priority-medium`

---

**调研生成时间**: YYYY-MM-DD
**调研版本**: v1.0
EOF
)"
```

**创建成功**：✓ Issue #xxx 已创建

---

### 阶段 6: 保存调研报告

**保存调研报告到 docs/research/**：

```bash
# 创建报告文件
cat > docs/research/pomodoro-research.md <<'EOF'
# 番茄钟功能调研报告

> 为 DivineSense 添加番茄钟（Pomodoro）计时功能进行的深度调研

**调研日期**: YYYY-MM-DD
**调研版本**: v1.0
**关联 Issue**: [#xxx](https://github.com/hrygo/divinesense/issues/xxx)

---

## 1. 调研背景

### 1.1 用户需求
用户希望在 DivineSense 中集成番茄钟功能，帮助保持专注，提高生产力。

### 1.2 相关调研
- [BEST_PRACTICE_CLI_AGENT.md](https://github.com/hrygo/divinesense/blob/main/docs/research/BEST_PRACTICE_CLI_AGENT.md) - CLI 交互最佳实践参考

---

## 2. 调研内容

### 2.1 技术可行性
- **可行性评级**: 高
- **技术方案**: 纯前端实现，使用 `setInterval` 计时，`localStorage` 持久化
- **架构影响**: 无影响，新增独立模块

### 2.2 竞品分析
| 产品 | 实现方式 | 可借鉴点 |
|:-----|:---------|:---------|
| Todoist | 集成番茄钟 | 与任务关联 |
| Notion | 独立番茄钟组件 | 简洁 UI |
| Forest | 游戏化体验 | 专注统计可视化 |

### 2.3 用户价值
- **解决的问题**: 帮助用户保持专注，提高生产力
- **目标用户**: 所有需要专注工作/学习的用户
- **使用频率**: 高（日常工作场景）

---

## 3. 功能方案

### 3.1 功能边界
**包含**：
- 25 分钟倒计时（可自定义 15-60 分钟）
- 开始/暂停/重置控制
- 短休息（5 分钟）/ 长休息（15 分钟）提示
- 浏览器桌面通知
- 每日完成的番茄数统计

**不包含**：
- 与笔记关联（未来迭代）
- AI 建议番茄数（未来迭代）
- 跨设备同步（未来迭代）

### 3.2 技术方案
**后端**：无

**前端**：
- 新增 `web/src/components/PomodoroTimer.tsx`
- 新增 `web/src/hooks/usePomodoro.ts`（计时逻辑）
- 新增 `web/src/pages/Pomodoro.tsx`（独立页面，可选）
- i18n: `pomodoro.*` keys

**数据存储**：
- `localStorage` 存储当前状态和统计数据

### 3.3 实施复杂度
- **工作量估算**: 0.5 人周
- **风险等级**: 低

---

## 4. 附录

### 4.1 参考资源
- [Web Notifications API](https://developer.mozilla.org/en-US/docs/Web/API/Notifications_API)
- Todoist 番茄钟实现

### 4.2 变更历史
| 日期 | 版本 | 变更内容 |
|:-----|:-----|:---------|
| YYYY-MM-DD | v1.0 | 初始版本 |
EOF
```

**更新 docs/research/README.md 索引**：

```bash
# 在 "## 📁 当前结构" 添加
├── pomodoro-research.md          # 番茄钟功能调研

# 在 "## 📊 文档分类 > 其他调研" 添加
| 文档 | 说明 |
|:---|:---|
| [pomodoro-research.md](./pomodoro-research.md) | 番茄钟功能调研 |
```

**完成**：调研报告已保存，Issue 已创建，调研流程结束。
