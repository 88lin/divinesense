# Surgical Slim 使用指南 v3.3

> DivineSense 代码瘦身专家 — 完整使用手册

---

## 目录

1. [快速开始](#快速开始)
2. [命令参考](#命令参考)
3. [分析范式](#分析范式)
4. [输出解读](#输出解读)
5. [最佳实践](#最佳实践)

---

## 快速开始

### 基本用法

```bash
# 全项目并行扫描（推荐）
/surgical-slim scan

# 深度分析特定模块
/surgical-slim analyze plugin/cron

# 对比两个模块
/surgical-slim compare plugin/email plugin/filter

# 验证假设
/surgical-slim verify "plugin/cron 被 AI 代理替代"

# 查看任务进度
/surgical-slim status
```

### 参数说明

| 参数 | 类型 | 可选值 | 默认值 | 说明 |
|:-----|:-----|:-------|:-------|:-----|
| `command` | string | scan, analyze, compare, verify, status | — | 分析命令类型 |
| `target` | string | 路径 | — | 目标路径（analyze/compare） |
| `targets` | array | 路径数组 | — | 多个目标（compare） |
| `depth` | string | quick, standard, deep | standard | 分析深度 |
| `include_tests` | boolean | — | false | 是否包含测试文件 |
| `parallel` | boolean | — | true | 是否并行执行 |

---

## 命令参考

### scan — 全项目并行扫描

并行扫描整个项目，识别潜在的死代码和未竟之事。

**执行流程**：
```
1. 识别项目结构（plugin/、ai/、server/、web/）
2. 为每个区域创建独立任务
3. 并行启动分析子代理
4. 聚合结果生成综合报告
```

**输出示例**：
```
[████████░░] 80% (4/5 完成)
  ✅ plugin/ 分析完成 → 3 个候选
  ✅ ai/ 分析完成 → 2 个候选
  ✅ server/ 分析完成 → 1 个候选
  🔄 web/ 分析中...
  ⏳ 死代码检测 等待中
```

### analyze — 深度分析指定路径

对特定路径进行语义理解 + 依赖分析 + 删除影响评估。

**分析框架**：
```
第一层：意图理解
  └── 这段代码为什么存在？

第二层：依赖性质
  └── 谁在使用它？强依赖还是弱依赖？

第三层：影响评估
  └── 删除后会发生什么？

第四层：置信度
  └── 结论有多可靠？
```

**输出模板**：
```markdown
## 分析对象: `plugin/cron`

### 🧠 意图理解
CronParser 用于解析 Cron 表达式，让用户通过标准语法创建定时任务。
当前产品中，用户通过自然语言创建日程（AI 代理解析）。
这表明 Cron 功能已被 AI 代理替代。

### 🔍 证据收集
| 证据类型 | 发现 | 置信度 |
|:-------|:-----|:------|
| 直接导入 | 0 处 | 高 |
| 配置引用 | 0 处 | 高 |
| 文档提及 | 0 处 | 高 |

### 🧮 影响推理
如果删除 `plugin/cron`:
├── 编译影响 ✅ 无
├── 运行影响 ✅ 无
├── 测试影响 ⚠️ 有 3 个测试文件
└── 文档影响 ✅ 无

### 🎯 结论
**风险等级**: 🟢 可安全删除
**置信度**: 高
**建议**: 删除代码，保留测试作为迁移参考
```

### compare — 对比两个模块

对比分析两个模块的使用情况和依赖关系。

**适用场景**：
- 判断两个模块是否有功能重叠
- 识别"旧实现 vs 新实现"的迁移状态
- 分析模块间的关系

**输出示例**：
```
对比: plugin/email/ vs ai/agent/

plugin/email/:
  ├── 最后修改: 2023-06-15 (1年前)
  ├── 引用: 0 处
  └── 状态: 🟢 死代码

ai/agent/:
  ├── 最后修改: 2024-01-20
  ├── 引用: 多处
  └── 状态: ✅ 活跃

结论: 旧实现可删除，新实现已完全替代
```

### verify — 验证特定假设

验证用户提出的假设或发现。

**示例假设**：
- "plugin/cron 被 AI 代理替代"
- "plugin/filter 形成孤立子图"
- "web/src/components/Unused.tsx 是死代码"

**输出格式**：
```
假设验证: plugin/filter 形成孤立子图

验证过程:
1. 检查外部引用 → 0 处
2. 检查内部引用 → 10 处（互相引用）
3. 绘制依赖图 → 无外部边

结论: ✅ 假设成立
置信度: 高
```

### status — 查看任务进度

查看当前正在进行的分析任务列表和状态。

**输出示例**：
```
当前分析任务:

ID  | 任务       | 状态    | 进度
----|-----------|---------|------
1   | plugin/   | ✅ 完成 | 100%
2   | ai/       | ✅ 完成 | 100%
3   | server/   | 🔄 进行中 | 60%
4   | web/      | ⏳ 等待中 | 0%
5   | 死代码检测 | ⏳ 等待中 | 0%

总体进度: [████░░░░░] 40% (2/5 完成)
```

---

## 分析范式

### v3.0 SOTA 分析 vs 传统方法

| 维度 | 传统方法 | SOTA 分析 |
|:-----|:---------|:----------|
| 理解深度 | 语法层面 | 语义层面 |
| 方法 | grep 搜索函数名 | 理解函数用途和上下文 |
| 引用分析 | 计算引用次数 | 分析引用性质（必要/可选） |
| 依赖追踪 | 静态导入 | 理解动态加载和配置 |
| 流程 | 遵循预定义规则 | 自主制定分析策略 |
| 输出 | 结论 | 推理链 + 置信度 |
| 执行 | 顺序 O(n) | 并行 O(k) |

### 语义理解示例

```
❌ 简单模式匹配:
  grep "Cron" → 0 结果 → 删除

✅ 语义理解:
  1. CronParser 的用途是什么？
     → 解析用户输入的定时表达式

  2. 产品中如何创建定时任务？
     → 用户通过自然语言："每天下午 5 点提醒我"
     → AI 代理解析并创建

  3. Cron 功能是否仍在路线图中？
     → 文档无提及，UI 无入口

  结论: 被 AI 代理替代的功能，可安全删除
```

### 因果推理示例

```
❌ 简单引用计数:
  plugin/filter 有 10 个引用 → 保留

✅ 因果推理:
  1. 谁引用了 plugin/filter？
     → plugin/filter/email_filter.go (8 处)
     → plugin/filter/base.go (2 处)

  2. 这些引用文件是否被使用？
     → email_filter.go: 0 外部引用
     → base.go: 仅被 email_filter.go 引用

  3. 形成孤立子图:
     plugin/filter/
         ├── base.go ←──────┐
         └── email_filter.go │ (内部循环)
                            └─→ 无外部连接

  结论: 孤立子图，整体可删除
```

---

## 输出解读

### 风险等级

| 图标 | 等级 | 含义 | 建议行动 |
|:-----|:-----|:-----|:---------|
| 🟢 | 可安全删除 | 无依赖，功能已替代 | 可直接删除 |
| 🟡 | 需评估 | 有边界情况或未竟之事 | 人工复核后决定 |
| 🔴 | 保留 | 仍有活跃使用 | 保留代码 |
| ⚪ | 不确定 | 证据不足 | 需要进一步调查 |

### 置信度

| 等级 | 含义 | 行动建议 |
|:-----|:-----|:---------|
| **高** | 多重证据支持，无不确定性 | 可直接执行 |
| **中** | 证据充分，但存在边界情况 | 建议人工复核 |
| **低** | 证据不足，或有潜在隐藏依赖 | 必须人工复核 |

---

## 最佳实践

### 1. 从扫描开始

对于新项目或长时间未维护的代码库，先执行全项目扫描：

```bash
/surgical-slim scan
```

这会给你一个整体的死代码概览，然后选择特定模块进行深度分析。

### 2. 使用深度分析获取详情

对于扫描发现的候选模块，使用深度分析：

```bash
/surgical-slim analyze plugin/cron --depth deep
```

### 3. 验证关键假设

在删除代码前，验证关键假设：

```bash
/surgical-slim verify "删除 plugin/cron 不影响编译"
```

### 4. 保守决策

有疑虑时保留代码，可通过 `--include_tests` 检查测试覆盖：

```bash
/surgical-slim analyze plugin/cron --include_tests
```

### 5. 利用并行执行

大型项目使用并行扫描，分析时间从 O(n) 降至 O(k)：

```bash
/surgical-slim scan --parallel true
```

---

## 相关文档

| 文档 | 内容 |
|:-----|:-----|
| **SKILL.md** | 核心定义和 system prompt |
| **METHODS.md** | 技术方法详解 |
| **REFERENCE.md** | DivineSense 项目特定模式 |

---

*版本: v3.3.0 | 理念: 理解 > 匹配，推理 > 规则，并行 > 串行*
